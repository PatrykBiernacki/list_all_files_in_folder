Script returns list of files in directory and subdirectories in notation (filename, directory, folder depth)"
To start call ListFiles.list_in_folder(directory).

Alternatively run script and input directory when prompted. 